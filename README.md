# STATIC_NAT_REVISION
Did static one to one nat used an ACL at the server facing router to avoid any private addresses reaching the servers and then did a static one to one natting on client facing router.
